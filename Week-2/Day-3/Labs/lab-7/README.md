# Lab 7 — PySpark Optimization (Ride-Sharing)

```bash
pip install -r requirements.txt
python optimized_pipeline.py
```

Optional baseline (no broadcast / filter-first): `python baseline_pipeline.py`.

## Gen AI / Ollama

No runtime **`--ai`** hook; use local LLM for code review and optimizations against **`STEPS.md`**. Helpers: **`../genai/README.md`**, index **`../README.md`**.

## Detailed steps & testing

See **`STEPS.md`** for prerequisites (Java), output paths, and Spark-focused `pytest` notes.
