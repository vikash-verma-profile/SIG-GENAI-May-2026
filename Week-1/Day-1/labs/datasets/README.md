# Datasets

This folder contains sample datasets you can use for exercises.

## Files

- `departments.csv`: department master data (`department_id` is the primary key)
- `employees.csv`: employee records (`department_id` references `departments.csv`)

